server:
  port: 8080
cors:
  originPatterns: http://localhost:8080,http://localhost:3000,https://www.teste.com
file:
  upload-dir: /home/gabrielli/Documentos/UploadDir
security:
  jwt:
    token:
      secret-key: 53cr37
      expired-lenght: 3600000
email:
  subject: "Default subject"
  message: "Default message"
spring:
  application:
    name: rest-with-spring-boot-and-java
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${JDBC_URL}
    username: root
    password: admin123
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
    show-sql: false
    open-in-view: false
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${EMAIL_USERNAME}
    password: ${EMAIL_PASSWORD}
    properties:
        mail.smtp.auth: true
        mail.smtp.starttls.enabled: true
        mail.smtp.starttls.required: true
  servlet:
    multipart:
      enabled: true
      file-size-threshold: 2KB
      max-file-size: 200MB
      max-request-size: 215MB
logging:
  level:
    root: INFO
    #net.sf.jasperreports.engine.*: TRACE
    #io.github.gabriellisartori: DEBUG
spring-doc:
  paths-to-match:
    - /api/**/v1/**
    - /auth/**
  swagger-ui:
    use-root-path: true
